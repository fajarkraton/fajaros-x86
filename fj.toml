[project]
name = "fajaros-nova"
version = "0.5.0"
description = "FajarOS Nova v0.5.0 Transcendence — x86_64 OS in Fajar Lang (9K+ LOC, 145 commands)"
authors = ["Fajar <fajar@primecore.id>"]
license = "MIT"

[build]
target = "x86_64-unknown-none"
entry = "kernel/main.fj"
output = "build/fajaros.elf"
optimization = "release"

[build.linker]
script = "linker.ld"
entry = "_start"

[build.bare-metal]
no-std = true
no-libc = true
static-link = true
