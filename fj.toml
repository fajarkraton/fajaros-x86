[project]
name = "fajaros-nova"
version = "2.1.0"
description = "FajarOS Nova v2.1.0 Zenith — x86_64 OS in Fajar Lang (36K LOC, 240+ commands, 139 modules)"
authors = ["Fajar <fajar@primecore.id>"]
license = "MIT"

[build]
target = "x86_64-unknown-none"
entry = "kernel/main.fj"
output = "build/fajaros.elf"
optimization = "release"

[build.linker]
script = "linker.ld"
entry = "_start"

[build.bare-metal]
no-std = true
no-libc = true
static-link = true
